<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/itemPatch"
    android:layout_width="match_parent"
    android:layout_height="?android:attr/listPreferredItemHeightSmall"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    android:longClickable="true"
    android:paddingTop="@dimen/margin_normal"
    android:paddingBottom="@dimen/margin_normal">

    <TextView
        android:id="@+id/titleText"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:gravity="start|center_vertical"
        android:lines="1"
        android:paddingStart="@dimen/margin_normal"
        android:paddingEnd="0dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/swingText"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/swingText"
        android:layout_width="@dimen/patch_swing_width"
        android:layout_height="0dp"
        android:gravity="start|center_vertical"
        android:lines="1"
        android:paddingStart="0dp"
        android:paddingEnd="@dimen/margin_normal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@id/titleText"
        app:layout_constraintRight_toLeftOf="@id/tempoText"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/tempoText"
        android:layout_width="@dimen/patch_tempo_width"
        android:layout_height="0dp"
        android:gravity="start|center_vertical"
        android:lines="1"
        android:paddingStart="0dp"
        android:paddingEnd="@dimen/margin_normal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@id/swingText"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
